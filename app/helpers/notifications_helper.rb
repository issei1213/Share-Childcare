module NotificationsHelper
  # def unchecked_notifications
  #   # 実装予定(未読時のアイコン)
  #   @notifications = Notification.where(["(visitor_id = ? or visited_id = ?) and checked = ?", current_user.id, current_user.id, false])
  # end
end
